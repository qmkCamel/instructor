# Widget 配置指南

**Widget 显示最近一次抽取的语录和你的选择，强化"站直"效果。**

---

## 🎯 设计理念

- 📝 每次看手机都看到那句话
- 🔴 如果逃避了，红色警告
- 🚫 不给你台阶

---

## 📱 Widget 类型

### 桌面（3种尺寸）
| 尺寸 | 内容 |
|------|------|
| 小 | 语录 + 行动状态 |
| 中 | 语录 + 出处 + 日期 |
| 大 | 完整展示 |

### 锁屏（3种样式）
| 样式 | 内容 |
|------|------|
| 圆形 | 状态图标 |
| 矩形 | 语录 + 状态 |
| 内联 | 一行提醒 |

### 状态颜色
| 选择 | 颜色 |
|------|------|
| 照做了 | 🟢 绿色 |
| 没照做 | 🟠 橙色 |
| 逃避了 | 🔴 红色 |

---

## 🔧 配置步骤

### 1. 添加 Widget Target
```
File → New → Target → Widget Extension
Product Name: InstructorWidget
不勾选 "Include Configuration Intent"
```

### 2. 配置 App Groups
两个 target 都要添加：
```
Signing & Capabilities → + Capability → App Groups
添加: group.com.edge.instructor
```

### 3. 添加共享文件
将 `Shared/SharedModels.swift` 添加到两个 target：
- ✅ instructor
- ✅ InstructorWidget

### 4. 运行测试
```
选择 InstructorWidget scheme → 运行
```

---

## 📊 数据流

```
主应用 (保存记录)
    ↓
SharedDataManager (App Group)
    ↓
Widget (显示最近记录)
```

---

## 🐛 常见问题

| 问题 | 解决 |
|------|------|
| Widget 不显示数据 | 检查 App Groups 配置 |
| 显示"去抽一句" | 先在主应用完成一次抽取 |
| 编译错误 | Clean Build Folder (⇧⌘K) |

---

## 📁 相关文件

```
Shared/SharedModels.swift           # 共享数据
InstructorWidget/
├── InstructorWidget.swift          # 桌面小组件
├── LockScreenWidget.swift          # 锁屏小组件
└── InstructorWidgetBundle.swift    # Widget 入口
```

---

**这不是内容消费，这是行动提醒。**

